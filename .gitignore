# 依赖（已在项目内安装，不入库）
node_modules/

# 环境变量 / 本地配置
.env
.env.local
*.local

# 日志
*.log
npm-debug.log*

# 系统 / 编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/

# 构建缓存（重定向到项目目录，避免占用 C 盘）
.cache/

# 打包产物（portable 版约 360MB，不入库）
dist/
